{
  "name": "tailwindcss-setup",
  "version": "1.0.0",
  "description": "Step 1: Run the following commands",
  "main": "index.js",
  "scripts": {
    "build" : "npx tailwindcss -i ./src/input.css -o ./dist/output.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}
